# Memory — 2026-03-31

## Session State (compaction flush — 08:20 UTC)

### Active PR
- PR #15 open: `feature/console-ui-v1` → https://github.com/koenswings/agent-console-dev/pull/15
- Latest commit: `fa201cb` — demo mode + display mode stick fixes
- 81 tests passing

### What was built (PR #15 contents)
- Solid.js + Chrome Extension MV3, TypeScript strict
- Three deployment modes: dev (Tailscale), extension, production web app from Engine port 80
- Light theme default, dark via `prefers-color-scheme`
- Demo mode: mock store (2 engines, 3 disks, 5 instances), toggled in settings
  - Defaults ON when no Engine hostname configured
  - Yellow DEMO badge shown in status bar (`.status-bar__demo-badge`)
  - Dynamic import of `createEngineConnection` — only loads when demo mode off
- Display modes: side panel / popup / standalone window, switched at runtime via ⚙ settings
  - Radio buttons save immediately via `handleModeChange()` (not deferred to submit)
  - Key: `displayMode` in `chrome.storage.local`; values: `sidePanel` (default), `popup`, `window`
- Storage helpers: `csGet`/`csSet` (chrome.storage.local + localStorage fallback)
- Exported helpers: `readStoredHostname()`, `readStoredDemoMode()`
- Background service worker: `setPanelBehavior({ openPanelOnActionClick: true })` for side panel
- Manifest permissions: `storage`, `tabs`, `sidePanel`, `windows`

### Waiting on Koen
- Koen instructed to run `git pull origin feature/console-ui-v1 && pnpm package` then install updated extension
- If confirmed working → merge PR #15 → task `5629de17` closes

### Completed this session
- PR #15 merged — Console UI v1 ✅
- PR #16 open: `feature/architecture-doc` → https://github.com/koenswings/agent-console-dev/pull/16
  - Adds `docs/ARCHITECTURE.md` and updates `docs/INDEX.md`
  - Task `e9491b4e` (architecture doc) addressed by PR #16
  - 81 tests still passing
- Task `5629de17` (Console UI v1) needs Koen to mark done on MC board — board gate blocks agent from closing from `inbox`

### Next tasks (in order)
1. Koen reviews and merges PR #16
2. Cross-agent task for Axle: `GET /api/store-url` endpoint on Engine HTTP (port 80) — Console production mode needs it

### Operational notes (sandbox discipline)
- After every `git push` from sandbox: `find /home/node/workspace/agents/agent-console-dev/.git -user root -exec chown node:node {} \;`
- After every sandbox build: `rm -rf /home/node/workspace/agents/agent-console-dev/dist`
- Vite cache: `/tmp/idea-console-vite-cache` (not `node_modules/.vite/`)
- `pnpm install` from sandbox must use `CI=true pnpm install`

### Memory/outputs branch
- Branch: `memory/updates`, PR #13 open (ongoing)
