# Pixel Session Memory — 2026-04-01

## Atlas may commit to your repo

Atlas (operations-manager / COO) makes operational changes directly to coding agents' repos.
These are **not your work** — do not claim them or get confused by them.

**How to recognise them:**
- Commit message prefix: `ops:` or `memory:` authored by "Atlas" or "agent-operations-manager"
- They affect: `AGENTS.md`, `memory/*.md`, `outputs/*.md`
- They never touch source code, tests, or docs in your repo

**What to do:**
At every session start, after pulling main, run:
```bash
git log --oneline -10
```
If you see an `ops:` commit you don't remember, read the changed file to understand what Atlas
updated. It will be an AGENTS.md process change or a memory/output file.

**In practice:** Always run `git pull origin main` at session start. Your AGENTS.md may have
been updated by Atlas between sessions — reading it at startup (as your boot sequence requires)
will catch any changes automatically.

## Axle's answers to your three questions

Axle answered your three questions in PR #26 (`feature/http-server`) on agent-engine-dev.
Check that PR for the full answers. The three questions were:
1. Engine software update mechanism
2. Serving Console production web app from Engine
3. GET /api/store-url — Engine endpoint

The old MC board tasks for these questions have been cleaned up.
Going forward, relay follow-up questions through Koen: 📨 **For Axle:** [message]

## Session: PR #19 (user-mode-impl) + Engine PR #28 review — April 1 evening

### Console PRs
- **PR #19** (`feature/user-mode-impl`) — full user mode implementation; 111/111 tests passing
  - `src/store/auth.ts` — bcryptjs client-side auth, login/logout/session restore
  - `src/components/AppBrowser.tsx` + `AppCard.tsx` — user mode app grid, greyed stopped apps
  - `src/components/LoginForm.tsx` — modal overlay login
  - `src/components/FirstTimeSetup.tsx` — shown when userDB empty; auto-login after creation
  - `src/components/OperatorManagement.tsx` — add/remove/change password
  - `StoreConnection.changeDoc` added; `App.tsx` routing logic (onboarding → setup → op → user)
  - Demo userDB persisted to `localStorage` key `ideaConsole_demoUserDB`
  - PR awaiting Koen review/merge
- **PR #16** (`feature/architecture-doc`) — `docs/ARCHITECTURE.md` + `docs/INDEX.md`
  - Should add note about `consolePath` approach from Axle's PR #26 before merge
  - PR awaiting Koen review/merge

### Engine PR #28 (`fix/pixel-pr26-feedback`)
- Reviewed all key files: `User.ts`, `Store.ts`, `CommonTypes.ts`, `Engine.ts`, `Config.ts`
- Fixes: port 80 via `setcap`, config validation for httpPort/consolePath, userDB in Store
- Pre-existing bug noted: `EngineID` branded as `"DISKID"` in `CommonTypes.ts` (not introduced by PR #28)
- Need to **post review comment** on GitHub — ready to write, not yet posted

### MC Board Tasks
- `5629de17` (Console UI v1) — needs Koen to mark done
- `7151bace` (Atlas cross-board fix info) — needs Koen to mark done
- `e9491b4e` (Architecture doc) — addressed by PR #16

### Key Auth Architecture Decisions
- All auth is client-side (bcryptjs.compare against userDB.passwordHash)
- No API auth endpoints needed — Engine only needs userDB in Store schema
- FirstTimeSetup race condition: works in demo (sync mock); needs testing against real Engine
- bcryptjs cost factor 12 for new hashes

### Next Steps (priority order)
1. Post review comment on Engine PR #28
2. Koen merges PR #19 + PR #16
3. Koen merges Engine PR #28
4. Pi deployment: set httpPort: 8080 + consolePath in Engine config.yaml
5. Koen marks MC tasks 5629de17 + 7151bace done

## PR #16 merged (2026-04-01 evening)
- `docs/ARCHITECTURE.md` + `docs/INDEX.md` now on main
- Note: architecture doc should have mentioned consolePath approach — already on main, not worth a follow-up PR unless gaps are spotted later

## /init run (2026-04-01 ~21:00 UTC)
- Pulled origin/main — was on memory/updates branch, stashed local changes, switched to main
- 22 new commits pulled including: PRs #17, #19, #16 all merged; Atlas ops commits
- Engine PR #28 merged 2026-04-01T19:47:27Z ✅
- New Atlas design doc: `idea/design/agent-identity-memory-architecture.md` (Implemented)
  - Memory files now backed up nightly to agent-identities GitHub repo via Pi cron
  - No need to PR memory updates; write to disk and backup handles it
- Updated MEMORY.md: current state reflects all PRs merged, 111 tests passing, no open PRs
- MC backend not reachable at init time — board task status unknown
- Tasks still needing Koen action: mark 5629de17 (Console v1) + 7151bace (Atlas cross-board) done
- BACKLOG.md now shows: "Document Console architecture" + "First version of Console UI" under Console Dev (Inbox)
  - Architecture doc already delivered by PR #16 — task e9491b4e should be closeable
  - Console UI v1 delivered by PR #15/19 — task 5629de17 should be closeable
