# Memory — 2026-03-29

## Tech Stack Design Doc

- Wrote `design/001-console-tech-stack.md` — Console UI framework comparison and decision
- PR open: https://github.com/koenswings/agent-console-dev/pull/12 (awaiting CEO merge)
- Veri review task created on her board (cross-agent)

## Decision confirmed

Solid.js + Chrome Extension is the selected stack. Key reason: fine-grained signal-based
reactivity matches the Engine's Automerge streaming update model exactly — only affected
DOM nodes re-render on each small event.

Flutter eliminated: cannot produce a Chrome Extension (hard blocker).

Mobile path: PWA on same codebase, if needed later.

## Task context

Koen asked whether Flutter + Antigravity was worth considering. Short answer: no.
Antigravity is a dev IDE tool, not a deployment target. Flutter has no Chrome Extension support.

## Design docs status

- PR #12 (tech stack) → merged by Koen ✅
- PR #14 (dev setup + testing) → open, awaiting approval

## Open

- PR #14 needs CEO merge before implementation begins
- Cross-agent task for Axle needed: `GET /api/store-url` endpoint on Engine HTTP server (port 80) so Console can discover Automerge document URL
- Architecture doc task (`e9491b4e`) should reference both design docs once PR #14 is merged

## Key implementation decisions confirmed

- No UI component library — custom CSS only
- Dev: `pnpm dev` (host: true) → Tailscale port 5173
- Build: `pnpm build` + `pnpm package` (zip → serve port 8080 → Koen downloads)
- Testing: Vitest unit + component, mock Engine, Playwright E2E headless on Pi
- Engine WS port: 4321
- Manual verification needs real Engine + docked App Disk (Pi currently has neither running)
